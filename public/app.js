const API=window.DREAMCANVAS_API||"";const form=document.querySelector("#profile"),status=document.querySelector("#status"),stories=document.querySelector("#stories");form.addEventListener("submit",async e=>{e.preventDefault();const data=Object.fromEntries(new FormData(form));status.textContent="Saving…";try{const r=await fetch(`${API}/profile`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(data)});if(!r.ok)throw Error();status.textContent="Saved. Your first story will arrive at 8 PM.";}catch{status.textContent="The profile is ready for the deployed API."} });async function load(){try{const r=await fetch(`${API}/stories`);if(!r.ok)throw Error();const d=await r.json();stories.innerHTML=(d.stories||[]).map(s=>`<article class="story"><div><p class="eyebrow">READY FOR BED · ${new Date(s.createdAt).toLocaleString()}</p><h2>${esc(s.title)}</h2><p>${esc(s.body)}</p></div>${s.imageUrl?`<img src="${s.imageUrl}" alt="Story illustration">`:""}</article>`).join("")}catch{}}function esc(x){return String(x).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#039;"}[c]))}load();